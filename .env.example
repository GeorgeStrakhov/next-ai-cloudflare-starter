# Next.js Public Environment Variables
# Copy this file to .env.local and fill in your values
# These variables are loaded by Next.js during development
# Server-side variables should go in .dev.vars

# App Configuration (auto-filled by setup script)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_APP_NAME=My App
NEXT_PUBLIC_APP_DESCRIPTION=My awesome application
NEXT_PUBLIC_SUPPORT_EMAIL=hello@your-domain.com

# R2 Storage (Public CDN URL)
# This must match S3_PUBLIC_ENDPOINT in .dev.vars
NEXT_PUBLIC_S3_ENDPOINT=https://cdn.your-domain.com
